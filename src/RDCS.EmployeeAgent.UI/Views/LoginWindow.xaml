<Window x:Class="RDCS.EmployeeAgent.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RDCS Employee Agent - Login"
        Height="450"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo/Title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
            <TextBlock Text="RDCS Employee Agent" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Sign in to continue" 
                       FontSize="14" 
                       Foreground="#7F8C8D"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Email -->
        <TextBlock Grid.Row="1" Text="Email" FontSize="12" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" 
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                 Padding="10" 
                 FontSize="13"
                 BorderBrush="#BDC3C7"
                 BorderThickness="1"
                 Background="White"/>

        <!-- Password -->
        <TextBlock Grid.Row="3" Text="Password" FontSize="12" FontWeight="SemiBold" Foreground="#34495E" Margin="0,15,0,5"/>
        <Grid Grid.Row="4">
            <PasswordBox x:Name="PasswordBox" 
                         PasswordChanged="PasswordBox_PasswordChanged"
                         Padding="10" 
                         FontSize="13"
                         BorderBrush="#BDC3C7"
                         BorderThickness="1"
                         Background="White"/>
            <Button Content="👁" 
                    HorizontalAlignment="Right" 
                    Margin="0,0,10,0"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Command="{Binding TogglePasswordVisibilityCommand}"
                    Width="30"/>
        </Grid>

        <!-- Remember Me -->
        <CheckBox Grid.Row="5" 
                  Content="Remember me" 
                  IsChecked="{Binding RememberMe}" 
                  Margin="0,15,0,0"
                  Foreground="#34495E"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="6"
                   Text="{Binding ErrorMessage}"
                   Foreground="#E74C3C"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,15,0,0"
                   Visibility="{Binding ErrorMessageVisibility}"/>

        <!-- Login Button -->
        <Button Grid.Row="7"
                Content="Login"
                Command="{Binding LoginCommand}"
                IsEnabled="{Binding IsLoginEnabled}"
                Margin="0,20,0,0"
                Padding="15"
                FontSize="14"
                FontWeight="SemiBold"
                Background="#3498DB"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2980B9"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#95A5A6"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
